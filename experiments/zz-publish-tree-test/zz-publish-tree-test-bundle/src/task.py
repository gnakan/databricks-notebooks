"""Fixture task. The self-test only reads its bytes; it is never executed."""


def main() -> None:
    """Print a line so the file is valid Python rather than an empty stub."""
    print("fixture task ran")


if __name__ == "__main__":
    main()
